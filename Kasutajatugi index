<?php
// /var/www/html/index.php
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);

require_once 'db.php';
$teade = '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $nimi = trim($_POST['nimi']);
    $osakond = trim($_POST['osakond']);
    $kontakt = trim($_POST['kontakt']);
    $kirjeldus = trim($_POST['kirjeldus']);

    // Serveripoolne valideerimine: tühje välju ei lubata
    if (empty($nimi) || empty($osakond) || empty($kontakt) || empty($kirjeldus)) {
        $teade = '<div class="alert alert-danger shadow-sm">Kõik väljad on kohustuslikud!</div>';
    } else {
        // PARANDATUD: Tabeli nimeks on poorduumised
        $stmt = $pdo->prepare('INSERT INTO poorduumised (nimi, osakond, kontakt, kirjeldus, staatus) VALUES (?, ?, ?, ?, "Uus")');
        if ($stmt->execute([$nimi, $osakond, $kontakt, $kirjeldus])) {
            $teade = '<div class="alert alert-success shadow-sm">IT-probleem on edukalt edastatud andmebaasi!</div>';
        } else {
            $teade = '<div class="alert alert-danger shadow-sm">Andmete salvestamisel tekkis viga!</div>';
        }
    }
}
?>
<!DOCTYPE html>
<html lang="et">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kasutajatugi - kasutajatugi.alari.local</title>
    <!-- Kohalik manustatud Bootstrap disain (veakindel ja offline-toimiv) -->
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; }
        body { background-color: #f8f9fa; color: #212529; line-height: 1.5; }
        .banner { background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), #6c757d; color: white; padding: 60px 20px; text-align: center; }
        .banner h1 { font-size: 2.5rem; font-weight: 700; margin-bottom: 8px; }
        .banner p { font-size: 1.25rem; font-weight: 300; }
        nav { background-color: #212529; padding: 12px 0; position: sticky; top: 0; z-index: 1020; box-shadow: 0 0.5rem 1rem rgba(0,0,0,0.15); margin-bottom: 24px; }
        .container { max-width: 1140px; margin: 0 auto; padding: 0 15px; display: flex; justify-content: space-between; align-items: center; }
        .navbar-brand { color: #0d6efd; font-weight: 700; text-decoration: none; font-size: 1.25rem; }
        .navbar-nav { display: flex; list-style: none; gap: 16px; }
        .nav-link { color: rgba(255,255,255,0.55); text-decoration: none; font-weight: 500; }
        .nav-link:hover, .nav-link.active { color: #fff; }
        .main-container { max-width: 720px; margin: 0 auto; padding: 0 15px 48px; }
        .card { background: white; border-radius: 6px; overflow: hidden; box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075); border: 0; }
        .card-header { background-color: #0d6efd; color: white; text-align: center; padding: 16px; }
        .card-header h4 { margin: 0; font-size: 1.5rem; font-weight: 500; }
        .card-body { padding: 24px; }
        .mb-3 { margin-bottom: 16px; }
        .form-label { display: block; margin-bottom: 8px; font-weight: 600; }
        .form-control { width: 100%; padding: 6px 12px; border: 1px solid #ced4da; border-radius: 4px; font-size: 1rem; }
        .form-control:focus { outline: 0; border-color: #86b7fe; box-shadow: 0 0 0 0.25rem rgba(13,110,253,0.25); }
        .btn-primary { display: block; width: 100%; padding: 8px 16px; background-color: #0d6efd; color: white; border: none; border-radius: 4px; font-size: 1rem; font-weight: 700; cursor: pointer; box-shadow: 0 0.125rem 0.25rem rgba(0,0,0,0.075); }
        .btn-primary:hover { background-color: #0b5ed7; }
        .alert { padding: 12px 16px; border-radius: 4px; margin-bottom: 16px; }
        .alert-danger { background-color: #f8d7da; color: #842029; border: 1px solid #f5c2c7; }
        .alert-success { background-color: #d1e7dd; color: #0f5132; border: 1px solid #badbcc; }
    </style>
</head>
<body>

    <!-- 1. BÄNNER (Teemakohane stiil ja pealkiri) -->
    <div class="banner">
        <h1>Kasutajatugi</h1>
        <p>alari.local ametlik IT-tugikeskus töötajatele</p>
    </div>

    <!-- 2. MENÜÜRIBA (Avaleht, KKK, Kontaktid) -->
    <nav>
        <div class="container">
            <a class="navbar-brand" href="index.php">IT-Tugi</a>
            <ul class="navbar-nav">
                <li><a class="nav-link active" href="index.php">Avaleht</a></li>
                <li><a class="nav-link" href="#" onclick="alert('KKK leht on arenduses!')">KKK</a></li>
                <li><a class="nav-link" href="#" onclick="alert('Kontakt: kasutajatugi@alari.local')">Kontaktid</a></li>
                <li><a class="nav-link" href="admin.php" style="color: #ffc107;">Admin Liides</a></li>
            </ul>
        </div>
    </nav>

    <!-- 3. SISUPLOKK ja VORM -->
    <div class="main-container">
        <div class="card">
            <div class="card-header">
                <h4>Esita IT-probleem</h4>
            </div>
            <div class="card-body">

                <!-- Vastuse või vea teavitus -->
                <?php echo $teade; ?>

                <form action="index.php" method="POST">
                    <div class="mb-3">
                        <label class="form-label">Sinu nimi</label>
                        <input type="text" name="nimi" class="form-control" required placeholder="Ees- ja perekonnanimi">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Osakond</label>
                        <input type="text" name="osakond" class="form-control" required placeholder="Nt. Raamatupidamine, Müük">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Kontakt (e-post või telefon)</label>
                        <input type="text" name="kontakt" class="form-control" required placeholder="nimi@alari.local või +372...">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Probleemi kirjeldus</label>
                        <textarea name="kirjeldus" class="form-control" rows="5" required placeholder="Kirjelda muret võimalikult täpselt..."></textarea>
                    </div>

                    <button type="submit" class="btn btn-primary">Saada pöördumine</button>
                </form>

            </div>
        </div>
    </div>
</body>
</html>
